- args:
    sql: CREATE TABLE "admin_org_table"("id" bigserial NOT NULL, "admin_username"
      text NOT NULL, "organization" text NOT NULL, "project" text NOT NULL, PRIMARY
      KEY ("id","admin_username") )
  type: run_sql
- args:
    name: admin_org_table
  type: add_existing_table_or_view
